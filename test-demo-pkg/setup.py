from setuptools import setup

setup(
   name='demo-pkg',
   version='1.0',
   description='A dem python module',
   author='lcapatina',
   author_email='lcapatina@test.com',
   packages=['demo-pkg'],  #same as name
)
